PORTFOLIO_CONFIG=config/config.yaml
PORTFOLIO_LOG_LEVEL=INFO
PORTFOLIO_DB_PATH=data/portfolio.duckdb

# Optional. OpenFIGI resolves ISIN <-> ticker for the asset-lookup feature.
# It works without a key (lower rate limit); set one to raise it. Must be
# exported as a real environment variable to take effect.
# OPENFIGI_API_KEY=
